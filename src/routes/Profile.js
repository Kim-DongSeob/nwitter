import React, {useState} from 'react';


const Profile = () => {
  const[todos, setTodos] = useState([])

  const Test = () => {
    setTodos((current)) => []
  }
  return (
    <span>Profile</span>
  );
}
export default Profile;